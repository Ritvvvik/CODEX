"""Enterprise agent application package.

Modules:
- enterprise_agent: weight-first feasibility flow
- data_pipeline_agent: dataset-first orchestration flow
- web_app: browser UI + API endpoints for both flows
"""
